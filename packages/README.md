# Shared Packages

This folder will hold shared Python packages extracted from CrowPilot Developer.

Planned split:

- `crowpilot-core` - provider routing, memory indexing, model client abstraction
- `crowpilot-security` - sensitive gate and approval flow
- `crowpilot-mcp` - MCP relay and registry logic
- `crowpilot-ui-assets` - reusable static assets and shared frontend slices

Current state: placeholder only. Code still lives under `backend/app/` until extraction starts.
